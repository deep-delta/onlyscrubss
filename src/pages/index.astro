---
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Only Scrubs - The Voice of Healthcare</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        --primary: #2563eb; --text: #1e293b; --text-light: #64748b;
        --bg: #ffffff;
      }
      
      body { margin: 0; font-family: system-ui, -apple-system, sans-serif; background: var(--bg); color: var(--text); }
      
      /* Navbar */
      nav { display: flex; justify-content: space-between; align-items: center; padding: 20px 24px; max-width: 1000px; margin: 0 auto; }
      .logo { font-weight: 800; font-size: 1.2rem; color: var(--text); text-decoration: none; }
      .nav-link { color: var(--text); text-decoration: none; font-weight: 500; margin-left: 20px; }
      
      /* Hero Section */
      .hero {
        text-align: center; padding: 80px 20px;
        background: radial-gradient(circle at center, #eff6ff 0%, #ffffff 70%);
      }
      h1 { 
        font-size: 3rem; margin-bottom: 20px; letter-spacing: -1px; 
        background: linear-gradient(to right, #1e3a8a, #3b82f6);
        -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      }
      .subtitle { font-size: 1.2rem; color: var(--text-light); max-width: 600px; margin: 0 auto 40px; line-height: 1.6; }
      
      .btn-group { display: flex; gap: 16px; justify-content: center; }
      .btn { padding: 14px 28px; border-radius: 50px; text-decoration: none; font-weight: 600; transition: transform 0.2s; }
      .btn:active { transform: scale(0.95); }
      .btn-primary { background: var(--primary); color: white; box-shadow: 0 4px 14px rgba(37, 99, 235, 0.3); }
      .btn-secondary { background: white; color: var(--text); border: 1px solid #e2e8f0; }

      /* Features */
      .features { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 30px; max-width: 1000px; margin: 60px auto; padding: 20px; }
      .feature-card { padding: 30px; background: white; border-radius: 20px; border: 1px solid #f1f5f9; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05); }
      .icon { font-size: 2rem; margin-bottom: 16px; display: block; }
      h3 { margin: 0 0 10px; }
      p { color: var(--text-light); line-height: 1.5; margin: 0; }

      @media(max-width: 600px) { h1 { font-size: 2.2rem; } }
    </style>
  </head>
  <body>
    <nav>
      <a href="/" class="logo">Only Scrubs</a>
      <div>
        <a href="/stories" class="nav-link">Read Stories</a>
      </div>
    </nav>

    <section class="hero">
      <h1>What happens on the ward,<br>stays on Only Scrubs.</h1>
      <p class="subtitle">The anonymous safe space for nurses, doctors, and healthcare workers to vent, share crazy patient stories, and support each other.</p>
      
      <div class="btn-group">
        <a href="/stories" class="btn btn-primary">Read the Feed</a>
        <a href="/create" class="btn btn-secondary">Share a Story</a>
      </div>
    </section>

    <section class="features">
      <div class="feature-card">
        <span class="icon">🔒</span>
        <h3>100% Anonymous</h3>
        <p>No accounts required. No tracking. Share your experiences without fear of judgement or professional repercussions.</p>
      </div>
      <div class="feature-card">
        <span class="icon">🩺</span>
        <h3>For Healthcare Only</h3>
        <p>A community that understands the code blues, the 12-hour shifts, and the coffee that tastes like mud.</p>
      </div>
      <div class="feature-card">
        <span class="icon">💬</span>
        <h3>Real Support</h3>
        <p>Connect with others who "get it." Laugh at the chaos and find support during the hard shifts.</p>
      </div>
    </section>
  </body>
</html>
