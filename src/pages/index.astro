---
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Only Scrubs</title>
    <style>
      :root { --primary: #2563eb; --text: #0f172a; }
      body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, sans-serif; background: #ffffff; overflow-x: hidden; }

      /* HERO SECTION WITH ANIMATED GRADIENT */
      .hero {
        min-height: 80vh;
        display: flex; flex-direction: column; align-items: center; justify-content: center;
        text-align: center; padding: 20px;
        background: linear-gradient(135deg, #eff6ff 0%, #ffffff 100%);
        position: relative; overflow: hidden;
      }
      
      /* Floating blobs for "fluid" feel */
      .blob {
        position: absolute; width: 500px; height: 500px; background: #3b82f6;
        border-radius: 50%; opacity: 0.1; filter: blur(80px);
        animation: float 10s infinite ease-in-out; z-index: 0;
      }
      .blob:nth-child(1) { top: -100px; left: -100px; animation-delay: 0s; }
      .blob:nth-child(2) { bottom: -100px; right: -100px; background: #8b5cf6; animation-delay: 5s; }

      @keyframes float { 0%, 100% { transform: translate(0, 0); } 50% { transform: translate(30px, 50px); } }

      /* CONTENT ANIMATIONS */
      .content { z-index: 1; opacity: 0; animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
      
      h1 {
        font-size: 3.5rem; font-weight: 800; letter-spacing: -2px; margin-bottom: 20px;
        background: linear-gradient(to right, #1e3a8a, #3b82f6);
        -webkit-background-clip: text; -webkit-text-fill-color: transparent;
        line-height: 1.1;
      }
      
      p { font-size: 1.25rem; color: #64748b; max-width: 600px; margin: 0 auto 40px; line-height: 1.6; }

      .btn-group { display: flex; gap: 16px; justify-content: center; }

      .btn {
        padding: 16px 32px; border-radius: 50px; text-decoration: none; font-weight: 700; font-size: 1.1rem;
        transition: all 0.2s cubic-bezier(0.34, 1.56, 0.64, 1); /* Bouncy transition */
        position: relative; overflow: hidden;
      }
      
      .btn-primary {
        background: #2563eb; color: white;
        box-shadow: 0 10px 20px -5px rgba(37, 99, 235, 0.4);
      }
      .btn-primary:hover { transform: scale(1.05) translateY(-2px); box-shadow: 0 15px 30px -5px rgba(37, 99, 235, 0.5); }
      .btn-primary:active { transform: scale(0.95); }

      .btn-secondary { background: white; color: #1e293b; border: 1px solid #e2e8f0; }
      .btn-secondary:hover { background: #f8fafc; transform: translateY(-2px); }

      /* FEATURES GRID */
      .grid {
        display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 30px;
        max-width: 1000px; margin: -50px auto 100px; padding: 20px; z-index: 2; position: relative;
      }
      
      .feature-card {
        background: white; padding: 30px; border-radius: 24px;
        box-shadow: 0 10px 40px -10px rgba(0,0,0,0.08);
        border: 1px solid rgba(255,255,255,0.5);
        backdrop-filter: blur(10px);
        opacity: 0; animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        animation-delay: 0.3s;
        transition: transform 0.3s ease;
      }
      .feature-card:hover { transform: translateY(-10px); }
      .feature-card:nth-child(2) { animation-delay: 0.4s; }
      .feature-card:nth-child(3) { animation-delay: 0.5s; }

      .icon { font-size: 2.5rem; margin-bottom: 15px; display: block; }

      @keyframes slideUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

      @media(max-width: 600px) { h1 { font-size: 2.5rem; } }
    </style>
  </head>
  <body>
    <div class="hero">
      <div class="blob"></div><div class="blob"></div>
      <div class="content">
        <h1>What happens on the ward,<br>stays on Only Scrubs.</h1>
        <p>The anonymous social network for healthcare workers. Share your vents, wins, and chaos safely.</p>
        <div class="btn-group">
          <a href="/stories" class="btn btn-primary">Read Stories</a>
          <a href="/create" class="btn btn-secondary">Post Anonymous</a>
        </div>
      </div>
    </div>

    <div class="grid">
      <div class="feature-card"><span class="icon">🤫</span><h3>100% Anonymous</h3><p>No names. No tracking. Just raw stories.</p></div>
      <div class="feature-card"><span class="icon">⚡</span><h3>Instant & Fast</h3><p>Loads in milliseconds. No bloat.</p></div>
      <div class="feature-card"><span class="icon">🩺</span><h3>Verified Vibe</h3><p>For those who know the struggle.</p></div>
    </div>
  </body>
</html>
