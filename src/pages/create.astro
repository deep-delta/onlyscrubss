---
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>New Story</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      body { margin: 0; font-family: system-ui, sans-serif; background: #f9fafb; color: #1f2937; }
      nav { background: white; padding: 16px; border-bottom: 1px solid #e5e7eb; display: flex; justify-content: center; }
      .logo { font-weight: 800; color: #1e3a8a; text-decoration: none; font-size: 1.2rem; }
      
      main { max-width: 600px; margin: 40px auto; padding: 0 16px; }
      .card { background: white; padding: 30px; border-radius: 16px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); }
      
      h1 { margin-top: 0; font-size: 1.5rem; }
      .field { margin-bottom: 20px; display: flex; flex-direction: column; }
      label { font-size: 0.9rem; font-weight: 600; margin-bottom: 8px; }
      
      input, select, textarea { padding: 12px; border-radius: 8px; border: 1px solid #d1d5db; font-size: 1rem; font-family: inherit; }
      input:focus, textarea:focus { border-color: #2563eb; outline: none; }
      
      button { background: #2563eb; color: white; border: none; padding: 14px; border-radius: 8px; font-weight: 700; font-size: 1rem; cursor: pointer; width: 100%; }
      .back { display: block; text-align: center; margin-top: 20px; color: #6b7280; text-decoration: none; }
    </style>
  </head>
  <body>
    <nav><a href="/" class="logo">Only Scrubs</a></nav>
    <main>
      <div class="card">
        <h1>Share your story</h1>
        <form id="form">
          <div class="field">
            <label>Category</label>
            <select name="category">
              <option>General</option><option>Vent 😤</option><option>Happy ✨</option><option>Advice 🤝</option><option>Crazy 🚑</option>
            </select>
          </div>
          
          <div class="field">
            <label>Title (Short & Sweet)</label>
            <input name="title" maxlength="100" placeholder="e.g. The patient who ate my lunch..." required />
          </div>

          <div class="field">
            <label>Your Story</label>
            <textarea name="text" rows="6" required placeholder="What happened?"></textarea>
          </div>

          <div class="field">
            <label>Name (Optional)</label>
            <input name="name" placeholder="Anonymous" />
          </div>

          <div class="field">
            <label>Attachment (Optional)</label>
            <input type="file" name="file" />
          </div>

          <button type="submit">Post Story</button>
        </form>
        <a href="/stories" class="back">Cancel</a>
      </div>
    </main>
    <script>
      document.getElementById("form").onsubmit = async (e) => {
        e.preventDefault();
        const btn = e.target.querySelector("button");
        btn.disabled = true; btn.innerText = "Posting...";
        await fetch("/api/stories", { method: "POST", body: new FormData(e.target) });
        window.location.href = "/stories";
      }
    </script>
  </body>
</html>
